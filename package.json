{
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "t": "yarn workspace shipjs test:prepare -d ../../../shipjs-test-example",
    "build": "yarn workspace shipjs-lib build",
    "test": "yarn workspace shipjs-lib test && yarn workspace shipjs-cli test"
  },
  "author": "Algolia <support@algolia.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "shipjs",
  "devDependencies": {
    "lerna": "3.15.0"
  }
}
