{
  "version": "0.0.7",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "yarn build",
    "build": "yarn workspace shipjs-lib build",
    "lint": "yarn workspace shipjs-lib lint && yarn workspace shipjs lint",
    "test": "yarn workspace shipjs-lib test && yarn workspace shipjs test",
    "tw1": "yarn workspace shipjs-lib test:watch",
    "tw2": "yarn workspace shipjs test:watch",
    "bootstrap": "./packages/shipjs-lib/tests/bootstrap.sh",
    "release:prepare": "npx shipjs prepare",
    "release": "npx shipjs release"
  },
  "author": "Algolia <support@algolia.com>",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "name": "shipjs",
  "devDependencies": {
    "lerna": "3.16.1"
  }
}
