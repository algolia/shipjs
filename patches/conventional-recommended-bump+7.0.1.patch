diff --git a/node_modules/conventional-recommended-bump/index.js b/node_modules/conventional-recommended-bump/index.js
index 2e88842..142bb63 100644
--- a/node_modules/conventional-recommended-bump/index.js
+++ b/node_modules/conventional-recommended-bump/index.js
@@ -67,7 +67,8 @@ function conventionalRecommendedBump (optionsArgument, parserOptsArgument, cbArg
       lernaTags: !!options.lernaPackage,
       package: options.lernaPackage,
       tagPrefix: options.tagPrefix,
-      skipUnstable: options.skipUnstable
+      skipUnstable: options.skipUnstable,
+      cwd: options.path
     }, (err, tags) => {
       if (err) {
         return cb(err)
@@ -78,7 +79,7 @@ function conventionalRecommendedBump (optionsArgument, parserOptsArgument, cbArg
         from: tags[0] || '',
         path: options.path,
         ...options.gitRawCommitsOpts
-      })
+      }, { cwd: options.path })
         .pipe(conventionalCommitsParser(parserOpts))
         .pipe(concat(data => {
           const commits = options.ignoreReverted ? conventionalCommitsFilter(data) : data
